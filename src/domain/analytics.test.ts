import { describe,expect,it } from "vitest";
import { categoryBreakdown,dailyNutrition,foodStatistics,macroCalorieBreakdown,type DatedEntry } from "./analytics";
import type { DayFoodEntry } from "./types";
const dated=(date:string,name:string,categoryId:string,calories:number,protein=10):DatedEntry=>({date,entry:{id:`${date}-${name}`,dayId:date,snapshot:{foodId:name,name,categoryId,quantity:100,unit:"g",calculationMode:"per100",baseQuantity:100,calories,protein,carbohydrates:20,fat:5},sortIndex:0,consumed:false,createdAt:date,updatedAt:date} as DayFoodEntry});
describe("analytics",()=>{const items=[dated("2026-07-01","Rice","carb",130),dated("2026-07-01","Fish","protein",100,20),dated("2026-07-02","Rice","carb",130)];it("aggregates daily nutrition",()=>{const days=dailyNutrition(items);expect(days).toHaveLength(2);expect(days[0]?.calories).toBe(230)});it("builds category and macro breakdowns",()=>{expect(categoryBreakdown(items,new Map([["carb","Carbs"]]))[0]).toMatchObject({label:"Carbs",value:260});expect(macroCalorieBreakdown(items).reduce((sum,item)=>sum+item.value,0)).toBe(535)});it("ranks foods and calculates average quantity",()=>{const foods=foodStatistics(items);expect(foods[0]).toMatchObject({name:"Rice",logs:2,averageQuantity:100,lastDate:"2026-07-02"})})});
